apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: valkey-operator
  namespace: valkey-operator-system
spec:
  releaseName: valkey-operator
  chartRef:
    kind: OCIRepository
    name: valkey-operator
    namespace: valkey-operator-system
  interval: 15m
  timeout: 5m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  # Defaults: generated by helmify from https://github.com/hyperspike/valkey-operator/blob/main/config/default/kustomization.yaml
  # values:
